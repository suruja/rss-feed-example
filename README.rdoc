= RSS Feed example

This application is simply an example of how the +rss-feed+ gem works. To update your models from RSS feeds two times a day, simply run:

  bundle exec whenever

Modify +config/schedule.rb+ to change the cron frequency. For more information, please visit: https://github.com/javan/whenever
